<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.penseapp.acaocontabilidade">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".login.view.activities.LoginActivity"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".chat.view.ChatsActivity" />
        <activity android:name=".chat.view.ContactsActivity" />
        <activity android:name=".chat.view.MessagesActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.penseapp.acaocontabilidade.chat.view.TabbedMainActivity" />
        </activity>
        <activity
            android:name=".chat.view.TabbedMainActivity"
            android:label="@string/title_activity_tabbed_main"
            android:launchMode="singleTop"
            android:theme="@style/AppTheme">
        </activity>
        <activity
            android:name=".news.view.NewsItemActivity"
            android:parentActivityName=".chat.view.TabbedMainActivity"
            android:theme="@style/AppTheme">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.penseapp.acaocontabilidade.chat.view.TabbedMainActivity" />
        </activity>

        <!-- [START firebase_service] -->
        <service
            android:name=".domain.MyFirebaseMessagingService">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT"/>
            </intent-filter>
        </service>
        <!-- [END firebase_service] -->

        <!-- [START firebase_iid_service] -->
        <service
            android:name=".domain.MyFirebaseInstanceIDService">
            <intent-filter>
                <action android:name="com.google.firebase.INSTANCE_ID_EVENT"/>
            </intent-filter>
        </service>
        <!-- [END firebase_iid_service] -->

        <!--Within the application component, metadata elements to set a default icon and color for notifications. Android uses these values whenever incoming messages do not explicitly set icon and icon color.-->
        <!-- Set custom default icon. This is used when no icon is set for incoming notification messages.
             See README(https://goo.gl/l4GJaQ) for more. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@android:drawable/ic_dialog_alert" />
        <!-- Set color used with incoming notification messages. This is used when no color is set for the incoming
             notification message. See README(https://goo.gl/6BKBk7) for more. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/accent" />
    </application>

</manifest>